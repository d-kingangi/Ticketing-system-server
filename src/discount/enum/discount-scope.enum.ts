/**
 * I've created this enum to define what a discount applies to.
 * This makes the Discount entity more flexible and scalable.
 */
export enum DiscountScope {
    EVENT = 'EVENT',
    PRODUCT = 'PRODUCT',
}
